import Evt from './evt'

/* 
 * a base class that allows for local storage of settings
 * as well as extension into other types of settings
 */
class Settings extends Evt {
  
}

export default Settings
